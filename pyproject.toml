[project]
name = "pisya-bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mypy>=1.16.0",
    "pydantic>=2.11.5",
]

[dependency-groups]
dev = [
    "ruff>=0.11.12",
]
